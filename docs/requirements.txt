# ── Website-Audit Sales Automation — backend dependencies ──
# Python 3.11+
# After installing, run:  playwright install chromium
# Versions are sensible floors; pin exact versions with `pip freeze` once the build is green.

# Web framework / API
fastapi>=0.115
uvicorn[standard]>=0.32
sse-starlette>=2.1          # Server-Sent Events for job progress

# Config / validation / models
pydantic>=2.9
pydantic-settings>=2.6
python-dotenv>=1.0
pyyaml>=6.0

# CLI
typer>=0.12

# Data store
SQLAlchemy>=2.0

# Scraping / HTTP / parsing
playwright>=1.48
httpx>=0.27
beautifulsoup4>=4.12
lxml>=5.3

# LLM (provider-agnostic via LangChain) — install the providers you test
langchain>=0.3
langchain-anthropic>=0.3
langchain-openai>=0.2
langchain-google-genai>=2.0

# Gmail API (Phase 1 sender)
google-api-python-client>=2.149
google-auth>=2.35
google-auth-oauthlib>=1.2
google-auth-httplib2>=0.2

# Observability / resilience
structlog>=24.4
tenacity>=9.0

# Testing
pytest>=8.3
pytest-asyncio>=0.24
